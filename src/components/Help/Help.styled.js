import styled from "styled-components";

export const StyledHelp = styled.div`
  display: flex;
  align-items: center;
  gap: 0.85em;
  color: crimson;

  svg {
    font-size: 1.25rem;
  }

  span {
    font-size: 0.875rem;
    font-weight: 700;
    letter-spacing: 1px;
    color: var(--c-black);
  }
`;
